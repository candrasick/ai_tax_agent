[pytest]
dotenv_files = .env 
markers = 
    integration: mark test as an integration test (potentially slow or requires external resources)
filterwarnings =
    # Ignore warnings about unknown config options (like dotenv_files)
    ignore:Unknown config option.*:pytest.PytestConfigWarning 